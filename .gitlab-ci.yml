include:
  project: gardenlinux/gardenlinux-package-build
  file: pipeline/pipeline-git.yml

build amd64:
  tags:
  - gardenlinux-build-amd64-large

build arm64:
  tags: 
  - gardenlinux-build-arm64-large

variables:
  SOURCE_NAME: 'metalbond'
  SOURCE_REPO: git@github.com:ironcore-dev/metalbond.git
  SOURCE_TAG_PREFIX: 'v'
  BUILD_ARCH_ALL: ''
  BUILD_REQUIRES_GO: 'true'
